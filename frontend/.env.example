# API Configuration
# Set to 'true' to use API mode (requires backend server running)
# Set to 'false' or omit to use static mode (default, no backend required)
VITE_API_MODE=false

# API URL (only used when VITE_API_MODE=true)
VITE_API_URL=http://localhost:3001